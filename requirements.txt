# Default dependencies are intentionally minimal and target:
# matcher = get_matcher("dedode-lightglue")
#
# For the previous "all models" dependency set, use:
#   pip install -r requirements-full.txt
torch>=2.0
torchvision>=0.15
opencv-python>=4.5.4
matplotlib>=3.3
kornia>=0.7.3
yacs>=0.1.8
huggingface_hub>=0.22.0
setuptools<81  # pkg_resources compatibility
